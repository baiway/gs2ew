# GS2 environment variables for testing locally on a laptop running Ubuntu.
# Source this before compiling or running GS2  `source ~/.gs2.sh`
#
# Prerequisites (install once):
#   sudo apt install libnetcdff-dev

export MAKEFLAGS=-IMakefiles
export GK_SYSTEM=gnu_ubuntu
